# Odoriba（踊り場）

家具が玄関から目的の部屋まで**運び込めるか**を判定するアプリ。既存の家具アプリは「部屋に置けるか」しか扱わず、「そこまで運べるか」を判定するものが存在しない。

出力は「通る／通らない」だけでなく、**上限サイズ**（「この階段なら幅195cmまで入ります」）と**ボトルネック＋不足量**（「2階の踊り場であと4cm足りません」）。

詳細は `docs/PRD.md`、対外説明は `docs/pitch.md`、判断の履歴は `docs/decisions.md`、実装の手引きは `docs/implementation-guide.md`。

---

## 技術的な核心

既存のモーションプランナーは**自由に浮遊する剛体**を仮定している。しかし実際に運ぶのは人間で、

- 運搬者の体も通路を占有する
- 階段の途中で家具を上下逆にはできない
- 手で持てる位置が限られる

**人間の保持拘束を計画に組み込むこと**が本プロジェクトの主張。

### 実装方針（最重要）

**状態空間に人間の姿勢を足さない。** 6DOF のまま、衝突判定器を可搬性オラクルに差し替える。

```
feasible(q) = collisionFree(A(q)) ∧ ∃h : carriable(q, h)
```

運搬者2人の姿勢を状態に加えると20DOF超になり、狭通路では解けない。判定器を差し替える形なら、プランナー本体を一切変更せずに済む。**研究の中身をオラクルに閉じ込めるのが設計の要。**

### オラクルの実装優先順位

1. **人体の占有**（最優先）— 運搬者をカプセル（半径0.2m / 高さ1.7m）で近似。実効断面が家具より大きくなることが、現実の失敗モードのほぼ全て
2. **静的保持可能性** — 傾き角の上限＋重心が把持点の間にあること。厳密なトルク計算は不要
3. **把持拘束** — 把持可能領域から2点。**持ち替え（multi-modal planning）はスコープ外**

---

## 開発順序（この順を守る）

### Phase 0 — 実測 ← ゲート（完了）

段ボール箱（60cm×42cm）を実際の階段・廊下で運び、箱単体では通り、人が持つと通らない箇所を確認済み（2026-08-15）。`phase1/phase1_demo.py` の定数はこの実測値に基づく。

### Phase 1 — Python のみ / 2D で最小実証（進行中）

`phase1/phase1_demo.py`: L字廊下を (x, y, θ) グリッド上で BFS 探索し、箱単体では通り人を付けると通らないことを可視化する。

- **狭通路のためランダムサンプリングRRTは実用速度で解けなかった。** 代わりに (x, y, θ) を細かく離散化し、全探索（BFS）する resolution-complete な手法に切り替えた。RRT-Connect への回帰は時間ができてから検討する
- **可視化を最初に作る。後回し禁止。** 経路探索は目で見えないとデバッグが不可能

### Phase 2 — 3D / 可搬性オラクル本体

性能が足りなくなってから OMPL / FCL を検討する。それまでは自作で通す。

### Phase 3 — Go でバックエンド

ジョブキュー・API・DB。計画は数秒〜数分かかるため**同期APIにしない**。Go は技術的必要ではなく戦略的選択（バックエンド志向のため）。Phase 1-2 で導入する理由はゼロ。

### Phase 4 — Swift でスキャン連携

**何を取り込めばいいかが確定してから着手する。**

---

## 環境

```bash
python -m venv .venv && source .venv/bin/activate
pip install numpy scipy trimesh open3d matplotlib
```

- **open3d** — メッシュ整形、`RaycastingScene.compute_signed_distance()` でSDF
- **trimesh** — 読み込み・凸分解の下ごしらえ

---

## 設計判断（要点）

- **環境表現は SDF**（BVH や Octree ではない）— 「あと4cm」を返すため、クリアランスがクエリの副産物として得られる
- **家具は外接直方体で表さない** — 対角線が効く場面が多すぎる。V-HACD で凸分解。分解状態（脚あり／脚なし等）は別の剛体として扱う
- **入力層は差し替え可能にする** — 手入力が基本機能（全端末で動く）、LiDARは手間を減らす拡張（iPhone 12 Pro以降のみ）。LiDAR前提で設計しない
- **探索の階層化** — Level 0: 幅の閾値判定で即答／Level 1: 2D計画／Level 2: 3D+可搬性オラクル（階段・段差のみ）
- **狭通路対策** — 廊下・階段の中心線近傍を重点的にサンプリングする骨格誘導

詳細と再検討条件は `docs/decisions.md`。

---

## 未解決の前提

**iPhoneのスキャンで階段の形状が十分な精度で取れるか、未検証。** 5cm超の誤差が出るなら寸法手入力に切り替える（それでも主張は成立する）。詳細は `docs/PRD.md` の Open Questions。

## やらないこと

- スキャナの自作（RoomPlanなど既存のコモディティを使う）
- 持ち替えを含む計画（multi-modal planning）
- 学習ベースのプランナー（正解データが存在しない）
- Phase 1 で OMPL / Go / Swift を導入すること
- 配置シミュレーション機能（既存アプリが多数あり、解かれている問題）

---

# コーディング規約

- **コード中のコメント・docstringは日本語で書く。** 変数名・関数名・print出力・ライブラリのAPIなど、日本語にすると不自然/壊れるものは対象外
- 一目的一コミットの粒度と同様、コメントの言語も「あとから読む人が迷わない」ことを優先する

---

# ブランチ戦略（Git Flow）

このプロジェクトは Git Flow を採用する。`C:\dev\CLAUDE.md` の GitHub Flow 記述より、本リポジトリではこちらを優先する。

## ブランチの種類と用途

| ブランチ名 | 分岐元 | マージ先 | 用途 |
|---|---|---|---|
| `main` | — | — | 本番リリース履歴のみ。タグを打つ。直接コミット禁止 |
| `develop` | `main` | — | 開発の統合ブランチ。直接コミット禁止、feature 経由でのみマージ |
| `feature/<説明>` | `develop` | `develop` | 新機能・調査・移行作業など通常の開発 |
| `hotfix/<説明>` | `main` | `main` と `develop` | 本番の緊急修正 |

**`release/<version>` ブランチは使わない。** developで複数人が並行して作業し、リリース準備だけを別ラインで進める必要が出てきたら導入を検討する。それまではリリース時に `develop` を直接 `main` にマージしてタグを打つ。

## 命名規則

- 形式: `<type>/<kebab-case-description>`
- 英小文字・数字・ハイフンのみ使用する
- 例:
  - `feature/rrt-connect-planner`
  - `feature/migrate-alpha-project-docs`
  - `hotfix/collision-check-off-by-one`

## 作業フロー

1. `develop` から `feature/<説明>` を切る（`develop` が無ければ `main` から作る）
2. feature ブランチでコミットを積む
3. `develop` にマージする（下記「PRについて」参照）
4. リリース時は `develop` を `main` に直接マージし、`vX.Y.Z` のタグを打つ
5. マージ後にブランチを削除する

## PRについて

**現在は1人での開発のため、PRは必須としない。** feature ブランチの作業が終わったら、ローカルで `develop`（または `main`）に `git merge --no-ff` し、そのまま push してよい。GitHub上のPRは「見返したい時に開く」任意の手段として使う。

共同開発者が加わったら、レビューを機能させるためPR経由必須の運用に戻すこと。

## ルール

- feature/hotfix ブランチへの作業コミットは、必ずそのブランチ上で行う（`main`/`develop` へ直接コミットしない）
- 1ブランチ = 1目的（複数の変更を混在させない）
- 個人名ブランチは使用しない
- コミットメッセージは `C:\dev\CLAUDE.md` の Conventional Commits 規約に従う
- **コミットは粒度高く分ける。** 1コミット＝1つの論理的変更とし、複数の変更をまとめた大きな一括コミットは避ける。あとから `git log` を読んだときに、何をどの順で変えたかが追えることを優先する

---

# 大きいバイナリの扱い

`docs/assets/` 以下（画像・GIF・プレゼン資料 `.pptx` など）は git 管理しない。`.gitignore` で除外し、ローカルの作業ファイルとしてのみ保持する。
